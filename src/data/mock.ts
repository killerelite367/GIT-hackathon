/**
 * Deprecated. Static mock data has been replaced by a real, persisted store.
 * First-run demo content now lives in `src/data/seed.ts` and is loaded through
 * `src/lib/storage.ts` into the app store (`src/store/StoreContext.tsx`).
 *
 * Kept as a stub only because this environment can't delete files on the
 * OneDrive mount. Safe to remove from your own machine.
 */
export {};
